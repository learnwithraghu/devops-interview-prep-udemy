My goal is to create a udemy video course on "Real world K8 and Helm debugging".
For this I want think of around 20 real world secenarios where K8 and Helm debugging is required.
Then build the code for that and setup this repo in a way that we have 20 folders with the name of the scenario and inside that folder we have the code for that scenario and a README.md file which explains the scenario and how to debug it.

Not just that the thing is its live debugging , So I will use my local laptop to deploy it on either rancher or docker k8 setup so all of this should be easily deployable on a local setup.

The flow will be, I show the students the scenario and then I will show them how to debug it. 

Each folder should have a clear instruction on how to deploy the scenario 1st which I will do before the video recording and then I will show the debugging steps in the video so this should be recorded in a different read me like every command I need to run and things to say everything. 

Brainstorming some scenarios now and comeup with a plan and topics for the course. No need to write the code now just the plan and topics for the course.