apiVersion: v1
kind: Service
metadata:
  name: subchart-demo
spec:
  type: {{ .Values.service.type }}
  selector:
    app: subchart-demo
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
